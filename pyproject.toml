
[project]
name = "taskline"
version = "0.1.0"
description = "A simple CLI task manager"
requires-python = ">=3.11"

[project.scripts]
taskline = "taskline.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
